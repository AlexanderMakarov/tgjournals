# Lombok configuration file
# This file configures Lombok's behavior for the project

# Enable lombok to generate constructor, equals, hashCode, and toString methods
lombok.anyConstructor.addConstructorProperties = true

# Enable lombok to generate getters and setters
lombok.getter.noIsPrefix = true
